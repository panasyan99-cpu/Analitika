# Validation 1.8.5

Проверено:

- фактический `NTR2` не заменяется расчётным значением;
- fallback остаётся доступен для старого формата без `NTR2`;
- произвольное имя `.xlsx`/`.xlsm` сохраняется и не влияет на разбор отчёта;
- `Green Agat` нормализуется в `Green Agate`;
- `Green Stones` объединяет зелёные камни только в навигации;
- итоговый Excel выгружает конкретный камень, а не название аналитической группы;
- синтетический отчёт с фактической колонкой NTR2 корректно разобран.

Профильная проверка: 27 тестов модуля заказа пройдены в headless-режиме со служебной заглушкой Streamlit.
