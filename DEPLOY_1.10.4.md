# Установка Analitika Web 1.10.4

Версия устанавливается полной заменой файлов поверх 1.10.3 и уже содержит изменения 1.10.2–1.10.3.

```bash
pip install -r requirements.txt
pytest -q
streamlit run streamlit_app.py
```

Коммит:

```bash
git add -A
git commit -m "Analitika Web 1.10.4 - Separate stone and pearl order groups"
```
