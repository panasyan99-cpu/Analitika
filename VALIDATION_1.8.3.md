# Validation 1.8.3

## Проверка бизнес-логики

- Специализированные тесты заказа: 17/17.
- Полный набор без Streamlit AppTest: 122 теста пройдены.
- Единственный UI AppTest не запускался в контейнере, потому что в среде отсутствует установленный пакет `streamlit.testing`; это ограничение среды проверки, не ошибка приложения.
- `py_compile` для `src/order_workflow.py`, `src/app_meta.py` и `streamlit_app.py` пройден.

## Проверка на исходном `Заказ.xlsx`

- Загружено 8 255 изделий.
- Построено 1 333 каменно-комплектных блока.
- Исходный `Set# 2 622` разделён на 12 независимых групп по нормализованному камню.
- В группе Blue Sapphire осталось только 43 сапфировых изделия.
- Ruby и London Topaz выведены отдельными карточками того же Set# и больше не влияют на состав сапфировой карточки.
- Категория, положительный/отрицательный ТВП и ошибки рассчитаны отдельно по каждой каменной группе.
