# Validation 1.9.7

## Automated tests

- `pytest -q`: **196 passed, 1 skipped**.
- Пропущен только необязательный Streamlit AppTest, недоступный в build-контейнере.
- `python -m compileall -q src streamlit_app.py`: успешно.

## SKU clipboard regression

Проверено чистыми тестами:

- `"  SFR17N124B-FPB   \n\t"` нормализуется в `SFR17N124B-FPB`;
- в HTML и JavaScript отсутствуют конечные пробелы исходного значения;
- Clipboard API получает точную очищенную строку;
- видимое значение HTML-экранируется;
- внутреннее значение SKU сохраняется без изменений;
- в разметке присутствуют основной Clipboard API и резервный механизм копирования.

## Scope

Изменение ограничено карточками распределения размеров колец. Расчёт рекомендаций, облачное сохранение, количества, Limited Order, валидация размеров и Excel-экспорт не менялись.
