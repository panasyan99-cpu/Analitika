# Deploy 2.5.14

1. Распаковать архив в корень приватного репозитория.
2. Проверить `version.json`: версия `2.5.14`.
3. Выполнить тесты: `pytest -q`.
4. Закоммитить и отправить изменения.

```bash
git add -A
git commit -m "ui: clarify management blocks and color comparison deltas"
git push
```
