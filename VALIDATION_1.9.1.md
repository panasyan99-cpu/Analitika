# Validation — Analitika Web 1.9.1

Дата проверки: 2026-07-23.

## Автоматические проверки

```text
pytest -q
153 passed, 1 skipped
```

Пропущен один необязательный тест Streamlit AppTest, когда соответствующая среда недоступна.

## Компиляция

```text
python -m py_compile src/order_persistence.py src/order_workflow.py streamlit_app.py
```

Результат: успешно.

## Новые регрессионные сценарии

Проверено:

- создание и обновление `orders-index.json`;
- метаданные количества изделий, SKU, Limited Order, этапа и статуса;
- два режима одного исходного заказа;
- полное удаление всех объектов workspace;
- удаление дополнительных резервных объектов по префиксу;
- сохранение записи индекса при частичном удалении;
- вывод остаточного ключа в ошибке;
- удаление обоих локальных черновиков и локальных Excel;
- наличие кнопок открытия, обновления, продолжения и удаления;
- отсутствие ручного uploader JSON;
- сохранение согласованного интерфейса остатков, рекомендаций и Limited Order.

## Ограничение проверки

Реальный bucket Cloudflare R2 не использовался в тестовой среде. S3-операции проверены на in-memory реализации с тем же контрактом ключей, индекса, восстановления и удаления. Для production токен R2 должен иметь права чтения, записи, перечисления и удаления объектов выбранного bucket.
