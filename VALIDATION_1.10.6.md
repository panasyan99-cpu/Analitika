# Validation 1.10.6

## Автоматические проверки

- `python -m compileall -q .`
- `pytest -q`: **212 passed, 3 skipped** в локальной среде без установленного Streamlit.
- Добавлен отдельный тест разбора четырёхмесячной выгрузки `01.03.2026–30.06.2026`; у всех позиций закрепляется окно 4 месяца.
- GitHub Actions устанавливает реальные зависимости, проверяет `pip check`, выводит версию Streamlit 1.60.0 и запускает настоящие AppTest.
- Проверены: timed autosave fragment, локальные fragments, пагинация, очистка старых Excel из session state, независимые mode-manifest, компактный индекс и ротация диагностики.

## Ручная проверка на рабочем отчёте

1. Загрузить отчёт 80–100 МБ.
2. Замерить принятие рекомендации, ручное количество, замок и размеры.
3. Убедиться, что обычное действие не вызывает полную перестройку всей страницы.
4. Оставить грязный черновик без действий минимум на 12 секунд и проверить облачное восстановление.
5. Несколько раз сформировать Excel после изменений и убедиться, что память не растёт из-за старых версий.
6. Одновременно сохранить каменный и жемчужный черновики одного отчёта и проверить оба режима в библиотеке.
