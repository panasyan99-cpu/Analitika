# Validation 1.8.6

Проверено:

- `Set# 00847` в Blue Sapphire содержит две позиции: кольцо с 5 продажами/ТВП 0 и серьги с 0 продаж/ТВП 3;
- при включённом и выключенном ТВП-фильтре комплект сохраняет обе позиции;
- вторичный `st.expander` «Есть товар в пути» удалён;
- новый source hash удаляет старый SQLite-черновик и старый сохранённый workbook;
- повторный source hash сохраняет текущий черновик;
- ключи количества, размеров и сверки различаются для разных source hash;
- `Other Topaz + WBT` нормализуется в `White Topaz`;
- `Other Topaz + MLBT` нормализуется в `Multi Blue Topaz`;
- `Other Stones + LAP` нормализуется в `Lapis Lazurite`;
- итоговый Excel не содержит групповых названий, когда конкретный камень распознан.

Результат headless-проверки: 136 тестов пройдено. Один отдельный Streamlit AppTest не запускался без установленного пакета `streamlit.testing`.
