# Validation — Analitika Web 2.0 Secure Order Timeline

Проверено:

- версия приложения остаётся 2.0;
- пароль не записан в исходный код и читается из Streamlit Secrets или переменной окружения;
- используется безопасное сравнение `hmac.compare_digest`;
- после пяти неверных попыток вход блокируется на 60 секунд;
- сессия требует повторного входа после длительного бездействия;
- автоматические заказы «Камни» и «Жемчуг» имеют независимые статусы и даты;
- статусы: «Заказ отправлен», «Заказ в работе», «Shipping», «Получен»;
- все статусы до получения окрашиваются жёлтым, полученный заказ — зелёным;
- даты этапов сохраняются в локальном SQLite и Cloudflare R2;
- последовательность дат валидируется;
- ручной заказ требует количество изделий больше нуля;
- старые поля `received` и `received_at` сохранены для обратной совместимости;
- обе кнопки «Открыть заказ» остаются основными золотыми действиями;
- PDF-руководство обновлено и визуально проверено на 34 страницах;
- `pytest`: 244 passed, 3 skipped;
- `compileall`: успешно.

Пропущенные тесты — Streamlit AppTest в локальной среде без установленного Streamlit. В GitHub Actions зависимости устанавливаются из `requirements.txt`.
