# Validation — Analitika Web 2.0 Clean Workspace

Проверено после удаления дублирующих подсказок из рабочих вкладок.

- версия приложения остаётся `2.0`;
- описательные карточки под загрузчиками общего анализа, Sonu и заказа поставщику удалены;
- значки справки у основных Excel-загрузчиков удалены;
- требования к файлам и логика сохранены во вкладках «Как с этим работать» и в `USER_GUIDE.md`;
- `python -m compileall` выполнен успешно;
- `234 passed, 3 skipped`;
- `streamlit==1.60.0`, `pyarrow==24.0.0`;
- лимит загрузки: 150 МБ на файл.
