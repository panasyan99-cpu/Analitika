# Validation 1.8.4

Проверено на исходном `Заказ.xlsx`:

- прочитано 8 255 строк изделий;
- сформировано 1 336 каменных комплектных групп;
- текстовые заголовки `Короны BS` и `Короны RUBY` распознаны как самостоятельные комплекты;
- в режиме «Камни» найдено 49 позиций с положительным ТВП;
- суммарное положительное ТВП — 138 изделий;
- фильтр `ТВП > 0` оставляет только положительные значения и сохраняет названия комплектов;
- отрицательный ТВП остаётся в режиме `ТВП ≤ 0` как ошибка;
- `src/order_workflow.py` успешно прошёл `py_compile`.

Полный Streamlit AppTest в текущей среде не запускался, поскольку пакет Streamlit не установлен.
