# Validation — Analitika Web 2.6.0

Дата: 31.07.2026

## Автоматические проверки

- `python -m compileall -q .` — успешно.
- `pytest -q --disable-warnings` — **319 passed, 5 skipped, 0 failed**.
- `python scripts/ui_action_inventory.py`:
  - действий — **105**;
  - без явного ключа — **0**;
  - повторяющихся литеральных ключей — **0**.

## Проверенные регрессионные сценарии 2.6.0

1. Ошибка обоих хранилищ не выдаёт ложное сообщение о сохранении и блокирует закрытие заказа.
2. Аудит скрывает секретные поля.
3. Схема серебра использует точные рабочие названия полей.
4. Частично упавший импорт поставки продолжается без дублирования товара и строки поставки.
5. Критические интерфейсные действия присутствуют в статическом реестре.
6. Контракты R2, диагностики, явного ремонта схемы и постоянных решений Sonu присутствуют в исходном коде.

## Проверка браузера

В репозиторий включён `scripts/e2e_smoke.py` и ручной workflow `.github/workflows/e2e-smoke.yml`.
Сценарий предназначен для фактически развёрнутого сайта и требует GitHub Secrets:

- `ANALITIKA_BASE_URL`;
- `ANALITIKA_PASSWORD`.

В текущем изолированном окружении реальный Streamlit-сервер версии проекта не был запущен, поэтому физический click-through браузером здесь не заявляется как выполненный. Скрипт должен быть запущен после деплоя или на staging.

## Ограничения проверки

- Автоматический smoke-тест намеренно не нажимает кнопки, создающие, изменяющие или удаляющие производственные данные.
- Полный destructive E2E должен выполняться только на тестовой копии Baserow и отдельном тестовом R2 prefix.
- Доступность внешних Baserow/R2 зависит от сети и текущих Secrets конкретного деплоя.
## Финальная упаковка

- Чистая папка релиза: **325 файлов** до `SHA256SUMS.txt`, **326 файлов** вместе с контрольными суммами.
- В пакет не включены `secrets.toml`, SQLite-черновики, runtime-журналы, `.pyc` и pytest-кэши.
- Проверка литеральных учётных данных: неожиданных паролей, токенов и ключей — **0**.
- `SHA256SUMS.txt` успешно проверен для всех файлов.
- `Analitika_USER_GUIDE.pdf`: **26 страниц**, шрифты DejaVu Sans/Mono встроены, визуально проверены первая и заключительные страницы.

