# Validation 1.9.6

## Automated tests

- `pytest -q`: **193 passed, 1 skipped**.
- Пропущен только необязательный Streamlit AppTest, недоступный в build-контейнере.
- `python -m compileall -q src streamlit_app.py`: успешно.

## Regression: weak SKU inside a stronger set

На реальном файле `Заказ.xlsx` проверен SKU `KR160210B-FPB`:

- категория комплекта: `Средние комплекты`;
- продажи: `2`;
- общий остаток: `1`;
- TT: `1`;
- 63: `0`;
- TVP: `0`;
- итоговая автоматическая рекомендация: **3**;
- правило: `weak_item`.

## Recommendation safety scan

На реальном файле разобрано **8 255** товарных строк.

- автоматических рекомендаций `1` или `2`: **0**;
- повторных автоматических заказов SKU с собственным положительным TVP: **0**;
- минимальная положительная автоматическая рекомендация: **3**.

## Unit summaries

Проверено чистыми тестами:

- серьги, кольца и подвески суммируются в штуках, а не по числу SKU;
- `SKU` считается отдельно;
- Limited Order и нулевые количества не входят в основной итог;
- локальный итог выбранного раздела и общий итог используют одну функцию расчёта.
